<resources>

    <!-- Base application theme. -->
    <style name="OriginalPurpleGreyTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- enable window content transitions -->
        <item name="android:windowActivityTransitions">false</item>

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorWhite</item>
        <item name="colorPrimaryDark">@color/colorDarkMagenta</item>
        <item name="colorAccent">@color/colorOriginalPurple</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:textColor">@color/colorBlack</item>
        <!-- specify enter and exit transitions -->
        <!--item name="android:windowEnterTransition">@transition/explode</item-->
        <!--item name="android:windowExitTransition">@transition/explode</item-->

        <!-- specify shared element transitions -->
        <item name="android:windowSharedElementEnterTransition">
            @transition/change_image_transform</item>
        <item name="android:windowSharedElementExitTransition">
            @transition/change_image_transform</item>

    </style>

    <style name="CollapsingToolbarLayoutExpandedTextStyle" parent="OriginalPurpleGreyTheme">
        <item name="android:textColor">@android:color/transparent</item>
        <item name="android:textSize">0sp</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
